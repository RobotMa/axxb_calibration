function [A, B] = AB_genRand(length, optiPDF, X, M, Sig, num)
%% Data generation for AX = XB problem

A = zeros(4, 4, length);
B = zeros(4, 4, length);

for i = 1:length
    
    B(:,:,i) = expm(se3_vec(mvg(M, Sig, 1)));
    A(:,:,i) = X*B(:,:,i)*X^-1;
    
end